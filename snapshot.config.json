{
  "_comment": "Settings for the automatic season snapshot. The script reads standings from the website's own public API endpoint - no database keys needed anywhere. If the endpoint path ever changes, update it here.",

  "standingsEndpoint": "https://cjlofficial.org/api/public/standings"
}
